import { initialState } from '../initialState';

export const modal = (status = { ...initialState.modal }, action) => {
    switch (action.type) {
        default:
            return { ...state };
    }
};
