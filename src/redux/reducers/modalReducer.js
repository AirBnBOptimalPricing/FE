const initialState = {
    sideDrawer: {
        show: false,
    },
};

export const modal = (state = initialState, action) => {
    switch (action.type) {
        default:
            return { ...state };
    }
};
